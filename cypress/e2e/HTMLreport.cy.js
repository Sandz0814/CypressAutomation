// cypress-mochawesome-reporter

/*

// Install Html Cypress Reporter
npm i cypress-mochawesome-reporter : Install in the terminal

// to put in config.js
reporter: 'cypress-mochawesome-reporter',
require('cypress-mochawesome-reporter/plugin')(on);

// to put in e2e support
import 'cypress-mochawesome-reporter/register';


*/

// Report can onpy run in the terminal